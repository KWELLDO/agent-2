- 插件开发遵循装饰器注册模式：工具使用 `@tool(name, description, schema)` 装饰函数，命令使用 `@command(name, help_text)` 装饰函数。
- 所有插件函数必须接收 `(args, ctx)` 或 `(arg_str, ctx)` 参数，并通过 `ctx` 对象进行状态读写与终端输出，禁止直接操作全局状态。
- 路径处理统一通过 `ctx.resolve(path)` 将相对路径映射到当前工作区根目录，确保文件操作的安全性。